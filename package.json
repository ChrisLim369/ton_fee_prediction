{
  "name": "ton-fee-prediction",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:worker": "wrangler pages dev docs",
    "check:worker": "tsc --noEmit -p tsconfig.worker.json && vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.10",
    "@cloudflare/workers-types": "^4.20260531.1",
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.95.0"
  }
}
